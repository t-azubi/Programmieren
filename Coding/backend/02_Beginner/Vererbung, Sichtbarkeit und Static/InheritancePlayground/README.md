# InheritancePlayground

Kleines Konsolenprojekt für:

- Vererbung
- `protected`/`public`-Sichtbarkeit
- Überschreiben mit `override`
- Nutzung von `static`-Hilfsmethoden

## Start

Im Ordner `src` ausführen:

```bash
dotnet run
```
