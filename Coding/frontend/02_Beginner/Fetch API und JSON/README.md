# Fetch API und JSON

Dieses Beispiel zeigt:

- JSON-Daten laden
- Ergebnis im DOM ausgeben
- Fehlerfall behandeln

Hinweis: Für `fetch` die Datei am besten über einen lokalen Server öffnen (z. B. Live Server).
